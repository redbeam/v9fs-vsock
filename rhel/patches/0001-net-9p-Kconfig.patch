--- a/net/9p/Kconfig	2026-03-03 23:10:03.000000000 +0000
+++ b/net/9p/Kconfig	2026-03-31 16:51:49.215042632 +0000
@@ -32,6 +32,14 @@
 	  This builds support for a transports between
 	  guest partitions and a host partition.
 
+config NET_9P_VSOCK
+	depends on VSOCKETS
+	tristate "9P VSOCK Transport"
+	help
+	  This builds support for a transport for 9pfs over
+	  virtual sockets (vsock). This is useful for communication
+	  between virtual machines and their host.
+
 config NET_9P_XEN
 	depends on XEN
 	select XEN_XENBUS_FRONTEND
